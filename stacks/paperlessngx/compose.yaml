# docker-compose file for running paperless from the Docker Hub.
# This file contains everything paperless needs to run.
# Paperless supports amd64, arm and arm64 hardware.
#
# All compose files of paperless configure paperless in the following way:
#
# - Paperless is (re)started on system boot, if it was running before shutdown.
# - Docker volumes for storing data are managed by Docker.
# - Folders for importing and exporting files are created in the same directory
#   as this file and mounted to the correct folders inside the container.
# - Paperless listens on port 8000.
#
# In addition to that, this docker-compose file adds the following optional
# configurations:
#
# - Instead of SQLite (default), PostgreSQL is used as the database server.
# - Apache Tika and Gotenberg servers are started with paperless and paperless
#   is configured to use these services. These provide support for consuming
#   Office documents (Word, Excel, Power Point and their LibreOffice counter-
#   parts.
#
# To install and update paperless with this file, do the following:
#
# - Copy this file as 'docker-compose.yml' and the files 'docker-compose.env'
#   and '.env' into a folder.
# - Run 'docker compose pull'.
# - Run 'docker compose up -d'.
#
# For more extensive installation and update instructions, refer to the
# documentation.

services:
  broker:
    image: docker.io/library/redis:8.10.1@sha256:8a1efc5f479551822b47424ccae982026b633f28818eab0387348120a61e10e2
    restart: always
    volumes:
      - /mnt/software/applications/paperlessngx/redisdata:/data
    labels:
      - docker-volume-backup.stop-during-backup=true
    networks:
      - default

  db:
    image: docker.io/library/postgres:18.6@sha256:86c951e05bf56c93d95d397747fb8820ac76cc3bedb78f43abd83eedbe3666ae
    restart: always
    volumes:
      - /mnt/software/applications/paperlessngx/pgdata:/var/lib/postgresql
    environment:
      POSTGRES_DB: paperless
      POSTGRES_USER: paperless
      POSTGRES_PASSWORD: paperless
    labels:
      - docker-volume-backup.stop-during-backup=true
    networks:
      - default

  webserver:
    image: ghcr.io/paperless-ngx/paperless-ngx:3.2.1@sha256:5fa76604a81df6945086e0837b14b56543d137e8ce4f311cc5d9ebe907e74e79
    restart: always
    depends_on:
      - db
      - broker
      - gotenberg
      - tika
    ports:
      - {{op://docker/TrueNas/config/services_ip}}:8001:8000
    healthcheck:
      test:
        - CMD
        - curl
        - -fs
        - -S
        - --max-time
        - "2"
        - http://localhost:8000
      interval: 30s
      timeout: 10s
      retries: 5
    volumes:
      - /mnt/software/applications/paperlessngx/data:/usr/src/paperless/data
      - /mnt/software/applications/paperlessngx/media:/usr/src/paperless/media
      - /mnt/software/applications/paperlessngx/export:/usr/src/paperless/export
      - /mnt/software/applications/paperlessngx/consume:/usr/src/paperless/consume
    env_file: compose.env
    environment:
      - PAPERLESS_URL=https://paperless.${TLD}
      - PAPERLESS_REDIS=redis://broker:6379
      - PAPERLESS_DBENGINE=postgresql
      - PAPERLESS_DBHOST=db
      - PAPERLESS_TIKA_ENABLED=1
      - PAPERLESS_TIKA_GOTENBERG_ENDPOINT=http://gotenberg:3000
      - PAPERLESS_TIKA_ENDPOINT=http://tika:9998
      - PAPERLESS_EMAIL_PARSE_DEFAULT_LAYOUT=2
      - PAPERLESS_APPS=allauth.socialaccount.providers.openid_connect
      - PAPERLESS_SOCIALACCOUNT_PROVIDERS={"openid_connect":{"SCOPE":["openid","profile","email"],"OAUTH_PKCE_ENABLED":true,"APPS":[{"provider_id":"pocket-id","name":"Pocket-ID","client_id":"{{op://docker/pocket-id/paperless/client_id}}","settings":{"server_url":"https://pocketid.famillephan.fr/.well-known/openid-configuration"}}]}}
    labels:
      # Traefik configuration
      traefik.enable: true
      traefik.http.routers.paperless.rule: Host(`paperless.${TLD}`)
      traefik.http.routers.paperless.entrypoints: websecure
      traefik.http.routers.paperless.tls.certresolver: myresolver
      traefik.http.services.paperless.loadbalancer.server.port: 8000
      docker-volume-backup.stop-during-backup: true
      # Homepage configuration
      homepage.group: Media & Data
      homepage.name: Paperless
      homepage.icon: paperless-ngx.svg
      homepage.href: https://paperless.${TLD}/
      homepage.description: Document management
    networks:
      default: {}
      traefik_proxy: {}

  gotenberg:
    image: docker.io/gotenberg/gotenberg:8.37@sha256:f29984bd1e226bf1b93ba90af06000afa8b315853e99d27b9aaa41b93f15c769
    restart: always
    # The gotenberg chromium route is used to convert .eml files. We do not
    # want to allow external content like tracking pixels or even javascript.
    command:
      - "gotenberg"
      - "--chromium-disable-javascript=true"
      - "--chromium-allow-list=file:///tmp/.*"
    networks:
      - default

  tika:
    image: docker.io/apache/tika:3.3.1.0@sha256:90b7fa1dc018434075fce9e1d9b88b1e3d0ea6979d0cf86e116c79a8073ae973
    restart: unless-stopped
    networks:
      - default

  sftp:
    image: atmoz/sftp:debian@sha256:2b314c149154fc640e82db466faab4e086994d22aa609a2b0cf125555690a436
    restart: always
    volumes:
        - /mnt/software/applications/paperlessngx/consume:/home/paperless/consume
        - ${PWD}/sftp_config/paperless/id_rsa.pub:/home/paperless/.ssh/keys/id_rsa.pub:ro
        - ${PWD}/sftp_config/sshd_config:/etc/ssh/sshd_config:ro
        - ${PWD}/sftp_config/ssh_host_rsa_key:/etc/ssh/ssh_host_rsa_key:ro
        - ${PWD}/sftp_config/ssh_host_ed25519_key:/etc/ssh/ssh_host_ed25519_key:ro
    ports:
        - {{op://docker/TrueNas/config/services_ip}}:2552:22
    command: paperless::1000

volumes:
  data: null
  media: null
  pgdata: null
  consume: null
  redisdata: null

networks:
  default: {}
  traefik_proxy:
    external: true
